[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dli-llm"
version = "0.1.0"
description = "NVIDIA DLI 강의용 LLM 클라이언트 (수강생 배포판)"
requires-python = ">=3.9"
dependencies = ["openai>=1.0"]

[tool.setuptools]
packages = ["dli_llm"]
